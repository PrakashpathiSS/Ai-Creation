#lambda functions are anonymous functions that can be used to define a function in a single line.

custom_calculator = {
    "add": lambda a, b: a + b,
    "subtract": lambda a, b: a - b,
    "multiply": lambda a, b: a * b,
    "divide": lambda a, b: a / b
}

